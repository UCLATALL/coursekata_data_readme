# Run this from the console (or source the script) from the folder where
# README.Rmd lives (or change the file name to point to that folder like
# "some/folder/on/computer/README.Rmd")
rmarkdown::render('README.Rmd', output_format = 'all')
